{{#each DomainEnums}}
using System;

namespace {{../Namespace}}.Shared
{
    {{#if IsFlags}}
    [Flags]
    {{/if}}
    public enum {{Name}}
    {{write '{'}}{{#each Values}}{{#unless @first}},{{/unless}}
        {{Name}}{{#if (isNumber IntegerValue)}} = {{IntegerValue}}{{/if}}{{/each}}
    }
}
//[{{Name}}.cs]
//##SPLIT##
{{/each}}