{{#each Interfaces}}
using System;
{{#each Namespaces}}
using {{../../Namespace}}.Interfaces.{{.}};
{{/each}}

namespace {{../Namespace}}.Interfaces{{#if Namespace}}.{{Namespace}}{{/if}}
{
    public interface {{Name}}{{#each Interfaces as |interface|}}{{#unless IsMixinOnly}}{{#if @first}} :{{else}},{{/if}}
        {{interface.InterfaceName}}{{/unless}}{{/each}}
    {
        {{#each Properties}}
        {{#unless Inherited}}
        {{Type}} {{Name}} { get; set; }
        {{/unless}}
        {{/each}}
    }
}
//[{{#if Namespace}}{{Namespace}}\\{{/if}}{{Name}}.cs]
//##SPLIT##
{{/each}}